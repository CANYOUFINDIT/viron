# 技术合同：服务维护、监控大盘与凭据中心

> 负责人：Codex。状态：`UNFROZEN`。Gate 1 完成前，Grok 不得实施 Schema、API 和跨模块重构。

## 1. 合同信息

| 字段 | 值 |
| --- | --- |
| 状态 | `UNFROZEN` |
| 负责人 | `[待填]` |
| 输入代码 commit | `[待填]` |
| UI/UX 版本 | `[待填]` |
| 冻结时间 | `[待填]` |
| 用户批准的例外 | `[待填]` |

## 2. 已确认的现状

- 当前存在 `tls_endpoints` 与 `tls_endpoint_web_entries`，而原始方案提出独立证书资产与端点模型。
- 当前服务维护客户端和服务端均为高耦合大文件。
- 已存在主机监控、部署监控、时序、告警、探针和 TLS 探测能力，应优先复用。
- 工作区曾存在未跟踪的 `EnvironmentMonitoringDashboard.vue`；最终处置必须在本合同中明确。

## 3. 决策记录

| ID | 决策 | 选择 | 理由 | 兼容/迁移影响 |
| --- | --- | --- | --- | --- |
| ADR-001 | 证书资产模型 | `[待定]` |  |  |
| ADR-002 | Web 入口关联方式 | `[待定]` |  |  |
| ADR-003 | 工作空间作用域 | `[待定]` |  |  |
| ADR-004 | 服务维护 Payload | `[待定]` |  |  |
| ADR-005 | 监控聚合与时序 API | `[待定]` |  |  |
| ADR-006 | 未跟踪监控组件处置 | `[待定]` |  |  |

## 4. 数据模型

### 实体与关系

`[填写最终实体、主键、外键、唯一约束、级联规则和工作空间边界]`

### SQLite Schema

`[待填]`

### MySQL Schema

`[待填，并说明与 SQLite 的等价性]`

### 迁移、回滚与幂等

`[填写旧数据识别、搬迁、重复执行、安全失败和兼容窗口]`

### 生命周期

`[填写证书轮换、指纹变化、端点删除、Web 入口删除、SSH 连接删除和孤立资产处理]`

## 5. API 合同

每个 API 必须定义权限、作用域、请求、响应、错误码、分页/点数限制和审计行为。

| API | 方法 | 权限 | 请求 | 响应 | 限制 |
| --- | --- | --- | --- | --- | --- |
| 证书列表 | `[待填]` |  |  |  |  |
| 证书/端点探测 | `[待填]` |  |  |  |  |
| 证书关联管理 | `[待填]` |  |  |  |  |
| 监控概览 | `[待填]` |  |  |  |  |
| 主机时序 | `[待填]` |  |  |  |  |
| 服务部署清单 | `[待填]` |  |  |  |  |

## 6. 客户端组件边界

`[填写页面、容器组件、展示组件、composable、API contract 和状态归属]`

必须明确：

- `ServiceMaintenancePanel.vue` 的拆分目标。
- 证书中心与 Web 入口如何共享类型和状态。
- 现有 Host/Deployment/TimeSeries 组件的复用方式。
- NOC 全屏生命周期和刷新控制归属。

## 7. 权限与安全

`[填写工作空间隔离、角色权限、TLS 探测限制、SSRF/注入防护、服务操作授权和审计]`

## 8. 性能合同

`[填写时间范围、最大点数、降采样、分页、并发、超时、刷新频率、缓存和 N+1 防护]`

## 9. 错误与兼容策略

`[填写旧客户端/旧数据兼容、局部失败、数据陈旧、探针离线和 API 版本行为]`

## 10. 测试合同

| 层级 | 必测内容 | 通过条件 |
| --- | --- | --- |
| 单元测试 | `[待填]` |  |
| 路由/数据库测试 | `[待填]` |  |
| 客户端组件测试 | `[待填]` |  |
| 用户流测试 | `[待填]` |  |
| 桌面启动/打包 | `[待填]` |  |

## 11. 阶段文件边界

### Phase 1 允许修改

`[待填]`

### Phase 2 允许修改

`[待填]`

### Phase 3 允许修改

`[待填]`

### 全阶段禁止范围

`[待填]`

## 12. 非目标

`[明确本次不实现的内容，防止 Agent 扩大范围]`

## 13. 冻结检查

- [ ] 审计中的开放问题均已决定或明确延期。
- [ ] UI/UX 的后端依赖都有合同或降级方案。
- [ ] SQLite/MySQL 迁移可验证且幂等。
- [ ] API 权限、限制和错误行为完整。
- [ ] 每阶段文件范围和验收条件明确。
- [ ] 用户批准所有重大范围变化。
- [ ] 文档状态已更新为 `FROZEN`。
